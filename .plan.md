# Implementation Plan

Repo guidance check: no `ARCHITECTURE.md`, `AGENTS.md`, or `CLAUDE.md` exists in this worktree.

1. File: [src/ui/components/PlayerPanel.tsx](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/src/ui/components/PlayerPanel.tsx)  
   Change: Remove the hidden-opponent fan path entirely. Delete `HiddenHandFan`, `showHiddenHandFan`, `hiddenFeaturedRailCard`, and the `data-has-fan` branch. Replace them with one rail flow that always uses the same horizontal hand container for both visible and hidden seats. For hidden seats, render face-down placeholders for the concealed hand count inline with that rail, then render the visible cards that are allowed to show in the same flow. Keep the existing face-up `CardView` path, hover state, drag behavior, `registerCardElement`, and hand-area anchor registration. Do not attach hover, drag, preview, or `registerCardElement` behavior to the face-down placeholders.  
   Why: This is the core behavior change. It makes hidden opponent hands use the same rail model as the local seat and removes the detached fan geometry.

2. File: [src/ui/arena-theme.css](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/src/ui/arena-theme.css)  
   Change: Remove the fan-specific rules for `.arena-hidden-hand-fan`, `.arena-hidden-hand-fan__cards`, `.arena-hidden-hand-fan__featured`, and the associated breakpoint overrides. Remove the `data-has-fan` branch from the hand rail styles and any fan spread width or fan step variables. Rework the hand-area styling so hidden seats use the same flex-based rail structure as visible seats, with a hidden-state modifier that only changes spacing and presentation for the face-down placeholders. Make the hidden card-back placeholders use the same hand-card width and height as normal hand cards, keep them in the normal flex flow, and keep them non-interactive. Preserve the existing horizontal scrolling, `scroll-padding-inline`, and hover clearance so the commander can scale without clipping on desktop and mobile. Update the existing `max-width: 980px` and `max-width: 760px` blocks so they only tune the shared hand rail and hand card sizes.  
   Why: The current CSS is where the fan look is encoded. The new layout only works if hidden hands are styled as a true rail, not as a rotated spread with a featured overlay.

3. File: [qa/new-game-hand-rail.md](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/qa/new-game-hand-rail.md)  
   Change: Rewrite the hidden-opponent portion of this manual test so it validates a hidden hand rail instead of a hidden hand fan. Update the expected results to assert that hidden opponent seats render a horizontal rail of face-down placeholders, that the commander is integrated into that rail, that there is no detached fan geometry, that no face-up card information leaks, and that the placeholders do not intercept interactions meant for visible cards. Keep the local-seat hand rail checks intact. Add explicit hover and horizontal-scroll checks so the reviewer can verify the interaction, not just the static layout.  
   Why: This doc already captures the main “new game” regression. It needs to match the new unified presentation so future manual checks do not re-encode the old fan behavior.

4. File: rename [qa/hidden-hand-commander-fanout.md](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/qa/hidden-hand-commander-fanout.md) to [qa/hidden-hand-commander-rail.md](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/qa/hidden-hand-commander-rail.md) and rewrite it in place  
   Change: Replace all fan-specific language with rail language. The failure notes should describe detached rail segments, commander cards floating outside the rail, clipping on hover, or leaked card content rather than fan spread issues. Keep the doc focused on the hidden commander behavior only.  
   Why: The existing doc validates the old layout. Renaming it makes the new acceptance criteria unambiguous and prevents the old fan framing from lingering in the docs.

5. File: none; product-level verification via Playwright  
   Setup: Launch the app, enter the fresh-game board state, and if the app does not start there automatically, open Match Settings and click `New Game`. Wait until all four seats, the board, and the hand rails are fully rendered before starting the check. Run the scenario in Playwright at a desktop viewport and again at a narrow mobile viewport, and capture a Playwright video for both runs.  
   Verification: On screen, the local seat and every hidden opponent seat should use the same horizontal hand-rail model. Hidden seats should show a row of face-down placeholders inline with the rail, with the commander integrated into that same row rather than floating beside or above it. Hover the leftmost, middle, and rightmost visible hand cards, hover the hidden commander, and scroll the hand rail horizontally far enough to inspect the full hand. Confirm that no card clips, no hidden fan geometry appears, the hidden placeholders never expose face-up card information, and the placeholders do not intercept pointer interactions intended for visible cards. The reviewer should be able to see the commander remain attached to the rail during hover and scrolling in the recorded video.  
   Why: This is the product-level proof that the layout change works in the actual UI, including the interaction behavior and the breakpoint-sensitive rendering.

6. File: none; verification only  
   Run `npm run lint` and `npm run build` after the code changes. Then re-run the Playwright acceptance check above before signoff.

Files that should not need changes:
- [src/ui/components/GameBoard.tsx](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/src/ui/components/GameBoard.tsx)
- [src/ui/utils/gameView.ts](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/src/ui/utils/gameView.ts)
- [src/ui/types.ts](/Users/ironsha/Desktop/projects/openarena/.worktrees/orchestrate-1774492624253/src/ui/types.ts)
- Engine files under `src/engine/`